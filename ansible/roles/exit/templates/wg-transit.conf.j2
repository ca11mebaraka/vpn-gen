[Interface]
Address = {{ exit_wg_transit_address }}
ListenPort = {{ exit_wg_transit_listen_port }}
PrivateKey = {{ exit_wg_transit_private_key_resolved }}

[Peer]
PublicKey = {{ exit_entry_split_wg_transit_public_key }}
AllowedIPs = {{ exit_entry_split_allowed_ips | join(', ') }}
