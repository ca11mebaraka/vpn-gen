#!/usr/bin/env bash
set -Eeuo pipefail

script_dir=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)
if [[ -f "$script_dir/load-env.sh" ]]; then
  # shellcheck disable=SC1091
  source "$script_dir/load-env.sh"
fi
exec python3 "$script_dir/wg-client-admin.py" "$@"
