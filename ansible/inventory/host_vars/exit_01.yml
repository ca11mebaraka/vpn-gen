host_notes:
  os: Ubuntu Linux
  public_ip: "{{ vpn_gen_exit_host }}"
  ssh_user: "{{ vpn_gen_exit_ssh_user }}"
  ssh_key: "{{ vpn_gen_ssh_key_exit }}"

exit_entry_split_wg_transit_public_key: ""

exit_entry_full_public_key: ""
exit_public_interface: ens3
